services:
  - type: web
    name: email-verifier
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python app.py"
    instances: 1
    region: oregon
    envVars:
      - key: FLASK_ENV
        value: production
